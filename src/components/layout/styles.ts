import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme: any) => {
  return {
    container: {
      margin: "auto",
      padding: "0px 102px",
    },
  };
});

export default useStyles;
